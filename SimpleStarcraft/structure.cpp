#include "structure.h"

Structure::Structure(const Vector& position_, int size_, float maxHealth_, const sf::Color& color_) : Entity(position_, size_, maxHealth_, color_) {}

void Structure::update(float dt)
{
	// todo
}
